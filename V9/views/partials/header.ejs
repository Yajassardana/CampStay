<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>YelpCamp</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/main.css">
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
            <a href="/" class= "nav navbar-nav navbar-brand">YelpCamp</a>
        </div>
        <div class="collapse navbar-collapse">
          <ul class=" nav navbar-nav navbar-right">
              <li><a href="/campgrounds">Campgrounds</a></li>
            <% if(!currentUser){%>   <!-- Checking if a user is logged in or not and showing  option accordingly-->
            <li><a href="/login">Login</a></li>
            <li><a href="/register">SignUp</a></li>
            <%}else{%>
            <li><a href="#">Currently signed in as <%=currentUser.username%></a></li>
            <li><a href="/logout">Logout</a></li>
            <%}%>
            </ul>
        </div>
        </div>
    </nav>
  <%if(error&&error.length>0){%>
  <!-- as an empty array is truthy -->
  <div class="container">
    <div class="alert alert-danger text-center" role="alert">
      <%=error%>
    </div>
  </div>
  <%}%>
  <%if(success!=null&&success.length>0){%>
  <!-- as an empty array is truthy -->
  <div class="container">
    <div class="alert alert-success text-center" role="alert">
      <%=success%>
    </div>
  </div>
  <%}%>
